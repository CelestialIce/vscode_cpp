#include <iostream>
#include<array>
using namespace std;

int main()
{
    array<double,3>run;
    cin>>run[0];
    cin>>run[1];
    cin>>run[2];
    cout <<run[0]<<" "<<run[1]<<" "<<run[2]<<" "<<(run[0]+run[1]+run[2])/3.0;
    return 0;
}
cin>>ch
ch!='8'
cin.get(ch)
while(cin.get(ch)){

}
ch=cin.get()
